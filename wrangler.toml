name = "restpoke"
compatibility_date = "2026-09-10"

# Sito statico servito da un Worker.
# not_found_handling = SPA: qualsiasi indirizzo ricade su index.html (routing lato client).
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
